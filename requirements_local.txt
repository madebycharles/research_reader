# Research Reader — local server dependencies (playback + paper management)
# No TTS, no torch, no spaCy — those live on RunPod.
# Install with: setup_local.bat

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
PyMuPDF>=1.23.0
aiofiles>=23.2.1
pydantic>=2.5.0
requests>=2.31.0
numpy>=1.24.0
soundfile>=0.12.1
